@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860

    Edit and Delete button are used at multiple page, instead we will create partial view and use ref. of that page when require
*@

@*object type*@

@model int

@{
    ViewData["Title"] = "Partial Page";
}

<td class="text-right">
    <div class="btn-group">
        <a class="btn btn-success rounded-0" asp-action="Edit" asp-route-id="@Model"><i class="fa-solid fa-pen-to-square"></i></a>
        <form method="post">
            <button type="submit" onclick="if (!confirm('Are you sure, you want to delete?')) { return false }" class="btn btn-danger rounded-0" asp-action="Delete" asp-route-id="@Model"><i class="fa-solid fa-trash"></i></button>
        </form>
    </div>
</td>
